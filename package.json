{
  "name": "socket",
  "version": "1.0.0",
  "description": "Websocket allow us to communicate from client and server with an open connection",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "dkato",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.5",
    "socket.io": "^2.0.3"
  }
}
